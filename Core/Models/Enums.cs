namespace Models;
public enum FlatType
{
    Owner = 1,
    Privatized,
    StateOwned
}

public enum VoteType
{
    For = 1,
    Against,
    Abstain
}

public enum LivingQuater
{
    Living,
    NonLiving
}